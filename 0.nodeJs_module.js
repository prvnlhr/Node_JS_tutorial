const { getSupesData } = require("./data");

const obj = {
  name: "John",
  age: 30,
  address: {
    city: "New York",
    country: "USA",
  },
};

let arr = [1, 2, 3, 4, 5, 6];

// console.log(obj);

console.dir(arr);
